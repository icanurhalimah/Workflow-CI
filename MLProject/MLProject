name: personality-svm

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "personality_dataset_clean.csv"}
    command: >
      python modelling_tuning.py --data_path {data_path}